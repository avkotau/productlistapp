/**
 * Design constants for responsive image scaling throughout the application.
 *
 * DESIGN_IMAGE_WIDTH:
 *   The original width of the full image as specified in the design (px).
 *
 * DESIGN_IMAGE_HEIGHT:
 *   The original height of the full image as specified in the design (px).
 *
 * DESIGN_CONTAINER_SIZE:
 *   The target size of the image container in the design (394x394 px)
 *   used to extract specific portions of the full image.
 */

export const DESIGN_IMAGE_WIDTH = 846;
export const DESIGN_IMAGE_HEIGHT = 1280;
export const DESIGN_CONTAINER_SIZE = 394;
